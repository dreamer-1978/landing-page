{
  "name": "site",
  "version": "1.0.0",
  "description": "landing page with css, flex, grid",
  "source": "index.html",
  "scripts": {
    "sass:watch": "sass --style=expanded --watch styles:dist",
    "parcel:watch": "parcel serve --dist-dir public",
    "parcel:build": "parcel build --dist-dir build --public-url ./"
  },
  "author": "serega",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.13.2",
    "parcel-packager-svg-sprite": "^2.0.1",
    "parcel-transformer-svg-sprite": "^2.0.1",
    "sass": "^1.82.0",
    "svgo": "^3.3.2"
  }
}
